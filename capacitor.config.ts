import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ZooBitat_Front_Mobile',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
